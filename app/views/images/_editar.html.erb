<div class="modal fade" id="EditarModal<%= @image.id %>" tabindex="-1" role="dialog" aria-labelledby="EditarTitulo" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="EditarTitulo">Compartelo con todos!</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <%= form_for @image do |f| %>
                    <div class="form-group">
                        <%= f.text_field :description, class: "form-control", placeholder: "Descripción" %>
                    </div>
                    <div class="form-group justify-content-center d-flex ml-auto">
                        <%= f.submit class: "btn btn-success col-md-4" %>
                    </div>
                <% end %>
            </div>
        </div>
    </div>
</div>